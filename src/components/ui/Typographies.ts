import styled from "styled-components";

export const Typo = styled.p`
  margin: 0;
  padding: 0;
`;

export const TitleTypo = styled(Typo)`
  font-size: 1.1rem;
  font-weight: 700;
`;

export const ScriptType = styled(Typo)`
  color: #9d9d9d;
  font-weight: 500;
  font-size: 1rem;
`;
